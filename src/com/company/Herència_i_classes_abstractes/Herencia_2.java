package com.company.Herència_i_classes_abstractes;

public class Herencia_2 {

    /** 1)
     * Declara la classe Empleat, la qual hereda de la classe Persona.
     * Volem que tengui un identificador, un sou i uns anys de pertinen ̧ca a l’empresa.
     * Implementa un m`etode per incrementar en un 5% el seu sou i un altre per
     * incrementar en 1 els anys de pertinen ̧ca.
     * */

    /** 2)
     * Declara les seg ̈uents classes, les quals hereden d’Empleat:
     *
     * • Programador. Volem saber en quin projecte treballa en aquest mo-
     * ment i quines tasques t ́e assignades.
     *
     * Implementa els seg ̈uents m`etodes:
     * – addTask(task): rep per par`amtre una tasca i l’afegeix a la llista
     * de les que ja t ́e.
     * – completeTask(task): rep per par`ametre una tasca i l’elimina de la llista.
     * – showTasks(): per veure la llista de tasques que t ́e assignades en aquest moment.
     * Es suficient que una tasca sigui representada per un  ́ String.
     * • Cap de projecte. Ens interessa saber una llista dels projectes que
     * est`a gestiona ( ́es suficient una estructura de tipus String).
     * Implementa un m`etode per veure els projectes que t ́e assignats.
     */

    /** 3)
     * Desenvolupa un programa que cre ̈ı una s`erie d’objectes de les classes que
     * has creat i que faci  ́us dels m`etodes implementats. */
}
